info:
  name: List Study Plan Courses
  type: http
  seq: 4

http:
  method: POST
  url: '{{base_url}}/study_plans/courses'
  body:
    type: json
    data: |-
      {
        "study_plan_id": 1,
        "page": 1,
        "page_size": 20
      }

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
