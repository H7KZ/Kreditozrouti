info:
  name: Course Scrape Status (SSE)
  type: http
  seq: 2

http:
  method: GET
  url: "{{base_url}}/courses/1/scrape/status"
  headers:
    - name: Accept
      value: text/event-stream

settings:
  encodeUrl: true
