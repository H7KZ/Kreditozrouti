info:
  name: Run Faculty Timetables Scrape
  type: http
  seq: 7

http:
  method: POST
  url: "{{base_url}}/commands/insis/faculty-timetables"
  auth:
    type: bearer
    token: "{{api_command_token}}"

settings:
  encodeUrl: true
