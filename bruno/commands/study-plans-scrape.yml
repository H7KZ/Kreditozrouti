info:
  name: Run Study Plans Scrape
  type: http
  seq: 3

http:
  method: POST
  url: '{{base_url}}/commands/insis/studyplans'
  body:
    type: json
    data: |-
      {
        "faculties": [],
        "periods": [],
        "queue_study_plans": false
      }
  auth:
    type: bearer
    token: '{{api_command_token}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
