info:
  name: Run Study Plans Scrape
  type: http
  seq: 3

http:
  method: POST
  url: "{{base_url}}/commands/insis/studyplans"
  auth:
    type: bearer
    token: "{{api_command_token}}"
  body:
    type: json
    data: |-
      {
        "faculties": [],
        "periods": [],
        "auto_queue_study_plans": true
      }

settings:
  encodeUrl: true
