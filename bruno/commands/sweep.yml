info:
  name: Run Gap Sweep
  type: http
  seq: 8

http:
  method: POST
  url: '{{base_url}}/commands/insis/sweep'
  body:
    type: none
  auth:
    type: bearer
    token: '{{api_command_token}}'

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
