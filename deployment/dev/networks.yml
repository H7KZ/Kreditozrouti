networks:
  traefik-network:
    name: traefik-network
    external: true
    driver: bridge


  mysql-dev-network:
    name: mysql-dev-network
    external: true
    driver: bridge


  dragonfly-dev-network:
    name: dragonfly-dev-network
    external: true
    driver: bridge
