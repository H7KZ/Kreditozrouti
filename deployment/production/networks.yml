# ==============================================================================
# Networks Configuration: Production
# Description: External network definitions for production environment.
# ==============================================================================

networks:
  mysql-network:
    name: mysql-network
    external: false

  redis-network:
    name: redis-network
    external: false
