# ==============================================================================
# Volumes Configuration: Production
# Description: Persistent volume definitions for production environment.
#              Naming: <project>-<engine>-volume-<env>. See NAMING.md (Infrastructure).
# ==============================================================================

volumes:
  kreditozrouti-mysql-volume-prod:
    name: kreditozrouti-mysql-volume-prod
    external: true

  kreditozrouti-redis-volume-prod:
    name: kreditozrouti-redis-volume-prod
    external: true
