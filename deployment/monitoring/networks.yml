# ==============================================================================
# Networks Configuration: Monitoring Stack
# Description: Defines the external network for the monitoring stack.
# ==============================================================================

networks:
  traefik-network:
    name: traefik-network
    external: true

  monitoring-network:
    name: monitoring-network
    external: true

  alloy-network:
    name: alloy-network
    external: true
